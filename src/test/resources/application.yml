spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${MYSQL_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASSWORD}
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create

jwt:
  token:
    secret: ${SECRET_KEY}